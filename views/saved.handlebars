<div class="parallax-container">
  <div class="section no-pad-bot">
    <br>
    <br>
    <h1 class="header center">Saved Articles</h1>
    <h2 class="header center">Your Saved Articles</h2>
  </div>
  <div class="parallax">
    <img src="/assets/images/background.jpg" alt="hockey rink" style="display: block;">
  </div>
</div>

<div class="container">
  {{!-- for each article in mongodb --}}
  {{#each articleData}}
  {{#if this.saved}}
  {{!-- create a row, column, and card --}}
  <div class="row">
    <div class="col s12">
      <div class="card">
        <div class="card-content grey lighten-4">
          <div class="row valign-wrapper">
            <div class="col s8">
              {{!-- title and summary from db go here --}}
              <span class="card-title">{{this.title}}</span>
              <p>{{this.summary}}</p>
            </div>
            <div class="col s4">
              <img class="responsive-img" src="{{this.image}}" alt="article image">
            </div>
          </div>
        </div>
        <div class="card-action valign-wrapper center-align blue-grey darken-1">
          {{!-- delete button --}}
          <div class="col s6 card-links">
            <a href="#" class="delete-from-saved-button">Delete From Saved</a>
          </div>
          {{!-- link to comments goes here --}}
          <div class="col s6 card-links">
            <a href="#" style="margin: 0px">Article Comments</a>
          </div>
        </div>
      </div>
    </div>
  </div>
  {{/if}}
  {{/each}}
</div>
